package main.services;

import main.services.IConnectionRequest;
import main.services.IHttpConnectionFactory;

public class HttpConnectionFactory implements IHttpConnectionFactory{
    public void handle(IConnectionRequest request) {

    }
}
